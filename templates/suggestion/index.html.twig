{% extends 'base.html.twig' %}

{% block title %} Utilisateurs {% endblock %}

{% block body %}
    {#        {% for user in users %}#}
    {#        <tr>#}
    {#            <td>{{ user.pseudo }}</td>#}
    {#            <td>{{ user.email }}</td>#}
    {#            <td>{{ user.ville }}</td>#}
    {#        </tr>#}
    {#        {% endfor %}#}

    {#    <a class="nav-link" href="{{ path('app_category_index', {'id': category.id}) }}">#}
    {#        {{ category.name }}#}
    {#    </a>#}

    <body>
    <div class="container">

        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">FilmAmi</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                    aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Suggestions</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link nav-link2" href="#">Bonjour Davy !</a>
                    </li>
                </ul>
            </div>
        </nav>

        <div class="row">
            <div class="col-1"></div>
            <div class="col-1">
                <img class="logo" src="img/logo12.jpg" alt="">
            </div>
            <div class="col-10">

                <h1>Voici les personnes près de chez vous </h1>
                <h1>qui partagent vos goûts cinématographiques!</h1>
                <hr>
                <h2>Vous pouvez :</h2>
                <h3>Visiter leur profil, et peut-être ensuite leur proposer d'être votre ami sur ce site.</h3>
                <h3>Ou directement leur envoyer un mail pour leur proposer une séance!</h3>
                <h1>Bon film !</h1>
                <hr>

                <!--          Container des 3 cartes pour traitement Ajax          -->
                <div class="row liste container">
                    <div>
                        <div class="card" style="width: 18rem;">
                            <img src="../img/placeholder.jpg" class="card-img-top" alt="...">
                            {#<img src="{{ asset('../img/placeholder.jpg') }}" alt="">#}
                            <div class="card-body">
                                {% for user in users %}
                                <h5 class="card-title">{{ user.pseudo }}</h5>
                                <h5 class="card-title">Paris</h5>
                                <h5 class="card-title">{{ user.email }}</h5>
                                <h5 class="card-title">Drame</h5>
                                <p class="card-text">Ici la zone de cours texte du champ "A propos"</p>
                                <a href="#" class="btn btn-dark">Voir le profil</a>
                                <hr>
                                <a href="#" class="btn btn-dark">Envoyer un mail</a>
                            </div>
                        </div>
                    </div>
                    <div>

                    </div>
                    <div>

                    </div>
                </div>
                <!--          Fin du container des 3 cartes pour traitement Ajax          -->

            </div>
        </div>
    </div>
    {% endfor %}
    </body>

{% endblock %}
